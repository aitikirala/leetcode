


Solution:

class Solution {
    public boolean checkRecord(String s) {
        int absent = 0;
        if(s.contains("LLL")){
            return false;
        }
        for(int i=0; i<s.length(); i++){
            if(s.charAt(i) == 'A'){
                absent++;
            }
            if(absent >= 2){
                return false;
            }
        }

        return true;
    }
}
